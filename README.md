# 私の Live Status Monitor 酱

> 这是把主人的 Computer 状态全部暴露给全世界的「视奸 Dashboard」的说！
> 现在我在做什么、用什么 App，全部都看得到哦。也就是所谓的完全监视状态，虽然稍微有点害羞，但如果是你的话...也可以哦。



## 🌟 那个...我能做到的事情 (Features)

* **超高速の心跳同步**
    每隔 3 秒就会向服务器发送一次信号，这就是所谓的 Dokidoki 感觉吗？主人的状态是 Real-time 更新的。

* **Cyberpunk 风格的衣装**
    为了配合夜晚工作的气氛，特意换上了暗黑系的界面。会有呼吸一样的光效，非常漂亮呢。

* **全机种对应**
    * **macOS**: 无论是正在写代码还是看视频，都能完美捕捉。
    * **Windows**: 就算是名字很奇怪的 `.exe`，我也能通过内置的魔法字典把它翻译成好听的名字（比如把 `msedge` 变成 `Microsoft Edge` 这样）。

* **寂寞的 Offline 模式**
    如果主人超过 30 秒没有理我（没有发送心跳），我就会自动变成「OFFLINE」状态睡觉去了... Zzz。

* **绝对安全的加密通信**
    为了防止奇怪的人捣乱，我们在通信时约定了只有两个人知道的 Secret Key。达咩，不可以随便偷看哦。

## 🛠 身体构造 (Tech Stack)

虽然看起来很复杂，其实我的构造很简单的说：

* **大脑 (Backend)**: Python (Flask) —— 只有它才能处理我的逻辑呢。
* **脸蛋 (Frontend)**: HTML5 + CSS3 —— 没有用沉重的框架，是纯天然的素颜（原生 JS）。
* **触手 (Client)**: Python (`psutil` 等库) —— 用来感知主人的每一个动作。

## 🚀 契约缔结 (Quick Start)

想要把我带回家的话，请按照下面的步骤来操作吧。

### 第一步：据点确保 (Server Side)

首先需要在一个永远不会关机的地方（云服务器）帮我安个家。推荐使用 Ubuntu 系统。

```bash
# 首先要帮我准备好 Python 环境呢
apt update && apt install python3-pip -y
pip3 install flask

# 然后就可以唤醒我了！
# 记得用 nohup 让我一直在后台运行哦
nohup python3 app.py > log.txt 2>&1 &
